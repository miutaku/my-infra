apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: oci-encode-worker
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/oci/apps/encode-worker
  prune: true
  sourceRef:
    kind: GitRepository
    name: my-infra
  dependsOn:
    - name: oci-external-secrets-config
