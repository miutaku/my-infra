apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oci-grafana-alloy
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  project: default
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: alloy
      targetRevision: "0.12.0"
      helm:
        releaseName: grafana-alloy
        valueFiles:
          - $values/k8s/oci/apps/grafana-alloy/values.yaml
    - repoURL: https://github.com/miutaku/my-infra.git
      targetRevision: main
      ref: values
    # namespace と ExternalSecret (grafana-cloud-credentials) を適用
    - repoURL: https://github.com/miutaku/my-infra.git
      targetRevision: main
      path: k8s/oci/apps/grafana-alloy
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
