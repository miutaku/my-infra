apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mirakurun-data
  namespace: app-mirakurun
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
