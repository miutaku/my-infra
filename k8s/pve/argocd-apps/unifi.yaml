apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unifi
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  sources:
    - repoURL: https://github.com/chrissnell/unifi-os-kubernetes.git
      targetRevision: main
      path: chart
      helm:
        releaseName: unifi
        valueFiles:
          - $values/k8s/pve/unifi/values.yaml
    - repoURL: https://github.com/miutaku/my-infra.git
      targetRevision: main
      ref: values
    - repoURL: https://github.com/miutaku/my-infra.git
      targetRevision: main
      path: k8s/pve/unifi/patcher
  destination:
    server: https://kubernetes.default.svc
    namespace: unifi
  # ArgoCD v3 does not include postRenderers in the CRD schema for multi-source apps.
  # initContainers/volumes are patched by a PostSync hook to fix UniFi Core's
  # default UV_THREADPOOL_SIZE=256 crash under the kubelet pod PID limit.
  # ignoreDifferences prevents ArgoCD from treating the hook's patch as drift.
  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: unifi-unifi-os-server
      namespace: unifi
      jsonPointers:
        - /spec/template/spec/initContainers
        - /spec/template/metadata/annotations
        - /spec/template/spec/volumes
        - /spec/template/spec/containers/0/env
        - /spec/template/spec/containers/0/livenessProbe
        - /spec/template/spec/containers/0/readinessProbe
        - /spec/template/spec/containers/0/volumeMounts
        - /spec/template/spec/hostNetwork
        - /spec/template/spec/dnsPolicy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
