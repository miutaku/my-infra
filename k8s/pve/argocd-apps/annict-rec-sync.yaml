apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: annict-rec-sync
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  source:
    repoURL: https://github.com/miutaku/my-infra.git
    targetRevision: main
    path: k8s/pve/annict-rec-sync
  destination:
    server: https://kubernetes.default.svc
    namespace: app-annict-rec-sync
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
