apiVersion: v1
kind: Service
metadata:
  name: mirakurun
  namespace: app-mirakurun-staging
spec:
  ports:
    - name: http
      port: 40772
      protocol: TCP
      targetPort: 40772
  type: ClusterIP
