name: Path Filter Test

on:
  push:
    paths:
      - 'k8s/**'
      - '.github/workflows/deploy_k8s.yml'

jobs:
  test-path-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Workflow was triggered by a push with path filter."