all:
  hosts:
    pve-x570:
      ansible_host: 192.168.0.115
      ansible_user: miutaku
      ansible_password: "{{ lookup('env', 'PVE_PASSWORD') }}"
      ansible_become_password: "{{ lookup('env', 'PVE_PASSWORD') }}"
      ansible_ssh_common_args: -o StrictHostKeyChecking=accept-new
